name: Build alpha on main

on:
  push:
    branches:
      - main
    paths:
      - alpha/**


jobs:
  build:
    name: build
    runs-on: ubuntu-20.04

    steps:
      - name: Say hello
        run: echo "Hello alpha"
